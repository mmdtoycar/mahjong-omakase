# Frontend build-time configuration for Vite.
# Shared across all developers. Override locally if needed (gitignored: ui/.env.local).
# In production this is also injected as --build-arg in CI/CD (see .github/workflows/deploy.yml).

# Google OAuth Client ID — must match backend's GOOGLE_CLIENT_ID
VITE_GOOGLE_CLIENT_ID=471645797225-qtqf1nlv8l807tblfhpa9d36p5q456l3.apps.googleusercontent.com
