# Frontend build-time configuration for Vite.
# Copy to ui/.env (gitignored) and set values for local development.
# In production these are injected as Docker --build-arg in CI/CD.

# Google OAuth Client ID — must match backend's GOOGLE_CLIENT_ID
VITE_GOOGLE_CLIENT_ID=
